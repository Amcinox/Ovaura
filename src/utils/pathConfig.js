const paths = {
  imagePath: (imageName) => `./src/assets/images/${imageName}`,
  videoPath: (videoName) => `./src/assets/video/${videoName}`,
  audioPath: (audioName) => `./src/assets/audio/${audioName}`,
};

module.exports = paths;
